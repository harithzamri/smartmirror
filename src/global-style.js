import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`
    html, body {
        font-family: Montserrat,Arial,sans-serif;
        -wekit-font-smoothing: antialised;
        -moz-osx-font-smoothing: grayscale;
       
    }
`;
